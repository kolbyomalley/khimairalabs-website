# Always pin to exact versions — no ^ or ~ ranges.
# This is enforced here so `pnpm add` never silently introduces a floating range.
save-exact=true

# Fail if the engines field is not satisfied.
engine-strict=true
